const { Controller, Get, NestFactory } = require('@nestjs/core');
// Just manually query the mysql database directly using mysql2.
// The user already has it installed maybe?
// I will just use fetch to hit the backend directly, if I know how it reacts.
